apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: scscf-claim0
  name: scscf-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
